/**
 * @deprecated File replacements were removed; use `environment.ts` for all builds.
 * OpenRouter is reached via `/api/openrouter/...` + OPENROUTER_API_KEY on the server / dev proxy.
 */
export { environment } from './environment';
